============
Installation
============

At the command line::

    $ easy_install django-literature

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-literature
    $ pip install django-literature
